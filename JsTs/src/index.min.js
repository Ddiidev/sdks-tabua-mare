const BASE_URL = "https://tabuamare.api.br/api/v2"; class HttpClient { async get(e, t = {}) { if ("undefined" != typeof window && window.fetch) { const s = await fetch(e, { headers: t }); if (!s.ok) throw new Error(`HTTP Error: ${s.status}`); return s.json() } if ("undefined" != typeof require) { const s = require("https"), r = new URL(e), n = { "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36", Accept: "application/json, text/plain, */*", "Accept-Language": "pt-BR,pt;q=0.9,en-US;q=0.8,en;q=0.7", Connection: "keep-alive", ...t }; return new Promise(((t, o) => { const i = s.request({ hostname: r.hostname, port: r.port || 443, path: r.pathname + r.search, method: "GET", headers: n }, (e => { let s = ""; e.on("data", (e => s += e)), e.on("end", (() => { e.statusCode >= 200 && e.statusCode < 300 ? t(JSON.parse(s)) : o(new Error(`HTTP Error: ${e.statusCode}`)) })) })); i.on("error", o), i.end() })) } throw new Error("Ambiente não suportado") } } function validateLatLng(e, t) { if ("number" != typeof e || "number" != typeof t) throw new Error("Latitude e longitude devem ser números"); if (isNaN(e) || isNaN(t) || !isFinite(e) || !isFinite(t)) throw new Error("Latitude e longitude devem ser números válidos"); if (e < -90 || e > 90) throw new Error("Latitude deve estar entre -90 e 90 graus"); if (t < -180 || t > 180) throw new Error("Longitude deve estar entre -180 e 180 graus"); return `[${e.toFixed(6)},${t.toFixed(6)}]` } function formatDays(e) { return Array.isArray(e) ? `[${e.join(",").replace(/\s/g, "")}]` : "string" == typeof e && !e.startsWith("[") ? `[${e}]` : e } class TabuaMareClient { constructor(e = {}) { this.baseUrl = e.baseUrl || BASE_URL, this.apiKey = e.apiKey || "", this.http = new HttpClient } authHeaders() { return this.apiKey ? { Authorization: `Bearer ${this.apiKey}`, "X-Api-Key": this.apiKey } : {} } async request(e) { return this.http.get(`${this.baseUrl}${e}`, this.authHeaders()) } async getStates() { return this.request("/states") } async getHarborsByState(e) { if (!e || "string" != typeof e) throw new Error("Estado inválido"); return this.request(`/harbor_names/${e.toLowerCase()}`) } async getHarbors(e) { if (!e || Array.isArray(e) && 0 === e.length) throw new Error("Informe pelo menos um ID de porto"); const t = Array.isArray(e) ? e.join(",") : String(e); return this.request(`/harbors/[${t}]`) } async getTabuaMare(e, t, s) { if (!e || !t) throw new Error("harborId e month são obrigatórios"); const r = encodeURIComponent(formatDays(s)); return this.request(`/tabua-mare/${e}/${t}/${r}`) } async getTabuaMareRange(e, t, s, r) { return this.getTabuaMare(e, t, `[${s}-${r}]`) } async getTabuaMareMonth(e, t) { return this.getTabuaMare(e, t, "[1-31]") } async getNearestHarborByState(e, t, s) { if (!e || "string" != typeof e) throw new Error("Estado inválido"); const r = validateLatLng(t, s); return this.request(`/nearested-harbor/${e.toLowerCase()}/${r}`) } async getNearestHarbor(e, t) { const s = validateLatLng(e, t); return this.request(`/nearest-harbor-independent-state/${s}`) } async getGeoTabuaMare(e, t, s, r, n) { if (!s || "string" != typeof s) throw new Error("Estado inválido"); if (!r) throw new Error("month é obrigatório"); const o = validateLatLng(e, t), i = encodeURIComponent(formatDays(n)); return this.request(`/geo-tabua-mare/${o}/${s.toLowerCase()}/${r}/${i}`) } async getUsage() { if (!this.apiKey) throw new Error("getUsage() requer apiKey: informe options.apiKey no construtor"); return this.request("/usage") } } "undefined" != typeof module && module.exports && (module.exports = { TabuaMareClient }), "undefined" != typeof window && (window.TabuaMareClient = TabuaMareClient);
